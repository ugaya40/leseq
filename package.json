{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace leseq build",
    "test": "yarn workspace leseq test",
    "pub": "yarn workspace leseq pub",
    "pub:Doc": "yarn workspace leseq-docs deploy"

  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "^2.5.1",
    "typescript": "^4.5.4"
  }
}
